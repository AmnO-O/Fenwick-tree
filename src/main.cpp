#include "UI.h"
#include <iostream>


int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    std::cout << "\n======================================\n";
    std::cout << "  Fenwick Tree Testing Environment\n";
    std::cout << "======================================\n";

    // Start the UI process
    process();

    return 0;
}
